import Button from "@/components/button";

export const metadata = {
    title: "Test MDX Post",
};

# Hello MDX
---
## Heading 2

### Heading 3

This is a test file that uses MarkdownX, so that I can write Markdown but also use React components inside, like the button below

<Button label="Button in between Markdown paragraphs" color="primary" />

Well, this is pretty cool

This is a list in markdown:
 
- One
- Two
- Three